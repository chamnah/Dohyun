#include <iostream>
using namespace std;

int main()
{
	/*
	1. 최소값
	2. 같은값 -> 찾으면 "찾았다" 출력 / 못찾으면 "못찾았다" 출력
	*/

	//배열 최대값
	/*
	int arr[5] = { 1,2,3,4,5 };
	int maxNum = INT_MIN;

	for (int i = 0; i < 5; i++)
	{
		// i == 4
		if (maxNum < arr[i])
		{
			maxNum = arr[i];
		}
	}
	*/
	// maxNum = 최대값


	// 최소값
	/*
	int minNum = INT_MAX;
	for (int i = 0; i < 5; i++)
	{
		// i == 4
		if (arr[i] < minNum)
		{
			minNum = arr[i];
		}
	}
	*/

	return 0;
}