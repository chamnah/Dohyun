#include <iostream>
using namespace std;

/*
1. 함수는 세미콜론이 없다. int main();(X)

*/

void main()
{
	int num = 0;
	num = num + 1; // 값 증가
	num += 1;
	++num;
	num++;
	/*
	반복문
	while문 / do while문 / for문

	1. while - 횟수 지정없이 반복
	2. for - 횟수가 지정된 경우
	if(true/false)
	{
	  true면 실행ㅓ
	}

	while(true/false)
	{
	  true면 실행
	}

	*/

	/*
	// 무한 반복 실행
	// 무한 루프
	while (true) 
	{
		cout << "반복 실행~!!" << endl;
	}

	for (;;) // 무한 반복
	{
		cout << "반복 실행~!!" << endl;
	}

	*/

	/*int num = 1;
	while (true)
	{
		cout << "반복 실행~!! : " << num << endl;
		if (num >= 10)
		{
			break;
		}
		num++;
	}*/
	num + 1;
	/*int num = 1;
	while (num <= 10)
	{
		cout << "반복 실행~!! : " << num << endl;
		num++;
	}*/
	
	/*
	While 문
	1. 200 ~ 600까지 출력
	
	2. 1 ~ 100까지 수 중 3의 배수만 출력
	
	+a
	*/
}